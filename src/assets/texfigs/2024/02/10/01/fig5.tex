\documentclass{cmn}
\usepackage{t-util,ts-arrow,td-interpret-diag}
\usetikzlibrary{positioning}

\begin{document}
  \begin{tikzpicture}[parseTree]
    \def\topText{\small\texttt{top}};

    \node (TokensA) at (0,0) { \TUfbox{1}\TUfbox{+}\TUfbox{2}\TUfbox{+}\TUfbox{3} };

    \node[right=14mm of TokensA,parseNode] (NodeB) { 1 };
    \node[above=3pt of NodeB] { \topText };
    \node[right=2mm of NodeB] (TokensB) { \TUfbox{+}\TUfbox{2}\TUfbox{+}\TUfbox{3} };

    \node[right=18mm of TokensB,parseNode,yshift=3mm+6pt] (NodeCTop) { + }
      child { node[parseNode] { 1 } }
      child { node[parseNode] { 2 } };
    \coordinate[yshift=-3mm-6pt] (NodeC) at (NodeCTop);
    \node[above=3pt of NodeCTop] { \topText };
    \node[right=10mm of NodeC] (TokensC) { \TUfbox{+}\TUfbox{3} };

    \node[right=22mm of TokensC,parseNode,yshift=6mm+12pt] (NodeDTop) { + }
      child {
        node[parseNode] { + }
        child { node[parseNode] { 1 } }
        child { node[parseNode] { 2 } }
      }
      child { node[parseNode] { 3 } };
    \coordinate[yshift=-6mm-12pt] (NodeD) at (NodeDTop);
    \node[above=3pt of NodeDTop] { \topText };

    \draw[StlthBase,shorten <=4mm, shorten >=4mm] (TokensA) -- (NodeB);
    \draw[StlthBase,shorten <=4mm, shorten >=12mm] (TokensB) -- (NodeC);
    \draw[StlthBase,shorten <=4mm, shorten >=16mm] (TokensC) -- (NodeD);
  \end{tikzpicture}
\end{document}
