---
import Base from "@/layouts/Base.astro";
import TopNav from "@/components/page/topnav/TopNav.astro";
import Footer from "@/components/page/Footer.astro";

interface Props {
  title: string;
  desc?: string;
  image?: ImageMetadata;
}
const { title, desc, image } = Astro.props;
---

<Base title={title} desc={desc} image={image}>
  <div class="mt-4 mb-8 flex flex-col gap-8">
    <TopNav />
    <slot />
    <Footer />
  </div>
</Base>
