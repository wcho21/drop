---
interface Props {
  class?: string;
}

const classNames = Astro.props.class ?? "";
---

<nav
  class={[
    "text-[12px] sm:text-[14px] font-light list-none flex",
    classNames,
  ].join(" ")}
>
  <slot />
</nav>
