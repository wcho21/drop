---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata,
  alt: string,
}
const { src, alt } = Astro.props;
---

<figure class="w-full">
  <Image
    src={src}
    alt={alt}
    quality={80}
    class="w-full md:object-scale-down md:w-fit md:mx-auto"
  />
</figure>
