---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";

type BlogEntryData = CollectionEntry<"blog">["data"];
interface Props extends BlogEntryData { }

const { title, date, summary, thumbnail } = Astro.props;
---

<div class="flex items-center gap-3">
  {
    thumbnail !== undefined && (
      <div class="flex-0 w-1/4 md:w-36">
        <Image
          src={thumbnail}
          alt="Thumbnail"
          quality="80"
          width="240"
          height="160"
          class="border border-zinc-50 rounded-md w-full object-cover"
        />
      </div>
    )
  }
  <div class="flex-1 text-zinc-900">
    <p class="text-lg font-bold">{title}</p>
    {
      summary !== undefined && (
        <div>
        <p class="text-zinc-700 font-regular">{summary}</p>
        </div>
      )
    }
    <p class="text-sm text-zinc-400 font-light">{date.toLocaleDateString("ko-KR")}</p>
  </div>
</div>
