export default {
  plugins: [
    {
      name: 'preset-default',
      params: {
        overrides: {
          // danger to remove viewbox
          removeViewBox: false,
        },
      },
    },
  ],
};
