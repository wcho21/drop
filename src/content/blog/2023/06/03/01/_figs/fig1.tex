\documentclass[tikz,border=5mm,12pt]{standalone}
\usepackage[fontsize=16pt]{fontsize}
\usepackage{lattice}

\begin{document}
  \begin{tikzpicture}[
    circ/.style={circle,fill,inner sep=1.5pt},
    dyckpath/.style={line width=0.75pt,line cap=round},
    shade/.style={draw=none,fill=\shadecolor}
  ]
    \begin{scope}
      \drawgrid

      \node[circ] at (0,0) {};
      \node[circ] at (4*\step,4*\step) {};

      \draw[dyckpath] (0,0)
        -- ++(2*\step,0)
        -- ++(0,\step)
        -- ++(\step,0)
        -- ++(0,2*\step)
        -- ++(\step,0)
        -- ++(0,\step);

      \node at (2*\step,-8mm) {(O)};
    \end{scope}

    \begin{scope}[xshift=48mm]
      \drawgrid

      \node[circ] at (0,0) {};
      \node[circ] at (4*\step,4*\step) {};

      \draw[dyckpath] (0,0)
        -- ++(2*\step,0)
        -- ++(0,2*\step);
      \draw[dyckpath,red] (2*\step,2*\step)
        -- ++(0,\step)
        -- ++(\step,0);
      \draw[dyckpath] (3*\step,3*\step)
        -- ++(\step,0)
        -- ++(0,\step);

      \node at (2*\step,-8mm) {(X)};
    \end{scope}
  \end{tikzpicture}
\end{document}
