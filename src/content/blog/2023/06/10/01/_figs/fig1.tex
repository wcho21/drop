\documentclass[tikz,border=5mm,12pt]{standalone}
\usepackage[fontsize=16pt]{fontsize}
\usetikzlibrary{arrows.meta}

\newcommand\sidelen{50mm}
\newcommand\surrogatelen{0.0625*\sidelen}

\begin{document}
  \begin{tikzpicture}
    \draw (0,0)
      -- ++(\sidelen,0)
      -- ++(0,\sidelen)
      -- ++(-\sidelen,0) node[above right] {BMP}
      -- cycle;

    \draw (\sidelen,3*\surrogatelen)
      -- ++(-0.5*\sidelen,0)
      -- ++(0,\surrogatelen)
      -- ++(0.5*\sidelen,0) coordinate (A);

    \draw[{Stealth[scale=1.2]}-] (A)+(0,-0.5*\surrogatelen) .. controls +(0:4mm) and +(180:4mm) .. ++(10mm,6mm)
      node[right,text width=45mm,align=center,yshift=-2.5mm] {Surrogates Block\\ \small(\texttt{U+D800}--\texttt{U+DFFF})};
  \end{tikzpicture}
\end{document}
